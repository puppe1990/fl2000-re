#!/usr/bin/env bash
# Run this in Terminal.app (Screen Recording permission). The agent shell
# often gets a black framebuffer from CGWindowListCreateImage.
set -euo pipefail
cd "$(dirname "$0")/.."
exec .venv/bin/python hagibis_re.py mirror --seconds 0 --monitor 1
